# Supply-chain security: install/postinstall等のライフサイクルスクリプトを既定で無効化する。
# 悪意のあるnpmパッケージ(または乗っ取られたパッケージ)がインストール時に
# 任意コードを実行するのを防ぐための防御策。
#
# 影響: postinstallでバイナリ取得やセットアップを行うパッケージ(例: cypress, husky等)は
# 正常に動作しなくなる場合がある。必要な場合は個別に `npx <pkg> install` 等で対応すること。
ignore-scripts true
